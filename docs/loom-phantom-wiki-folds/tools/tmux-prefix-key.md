## Other prefixed commands

Every tmux command follows the same pattern after the remap: `Ctrl+a` then `c` opens a new window, and `Ctrl+a` then `d` detaches. The `Ctrl+a` choice mirrors the prefix used by GNU Screen and is easier to reach than the default `Ctrl+b`.
